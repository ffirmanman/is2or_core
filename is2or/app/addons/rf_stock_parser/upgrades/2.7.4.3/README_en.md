[!] Fixed the condition for the “If amount in price list equal 0” setting. Now not only 0, but also an empty value
[+] Fixed an error in starting cron if only the hour is specified. Now you can specify frequencies separated by commas, for example "3.10"
[!] Fixed the counter of enabled products in the logs
